spec:
  name: seal-hub-prover
  services:
    - name: seal-hub-prover
      github:
        branch: main
        deploy_on_push: true
        repo: https://github.com/avr6ude/seal-hub-prover.git
      dockerfile_path: Dockerfile.backend.production
  databases:
    - name: mongodb
